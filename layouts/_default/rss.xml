{{- $authorEmail := "" }}
{{- with site.Params.author }}
  {{- if reflect.IsMap . }}
    {{- with .email }}
      {{- $authorEmail = . }}
    {{- end }}
  {{- end }}
{{- end }}

{{- $authorName := "" }}
{{- with site.Params.author }}
  {{- if reflect.IsMap . }}
    {{- with .name }}
      {{- $authorName = . }}
    {{- end }}
  {{- else }}
    {{- $authorName  = . }}
  {{- end }}
{{- end }}

{{- $pctx := . }}
{{- if .IsHome }}{{ $pctx = site }}{{ end }}
{{- $pages := slice }}
{{- if or $.IsHome $.IsSection }}
{{- $pages = $pctx.RegularPages }}
{{- else }}
{{- $pages = $pctx.Pages }}
{{- end }}
{{- $pages = where $pages "Params.hiddenInRss" "!=" true -}}
{{- $pages = where $pages "Section" "!=" "news" -}}
{{- $limit := site.Config.Services.RSS.Limit }}
{{- if ge $limit 1 }}
{{- $pages = $pages | first $limit }}
{{- end }}
{{- $includePublications := or .IsHome (eq .Section "publications") }}
{{- $includeDiscoveries := or .IsHome (eq .Section "discoveries") }}
{{- $publications := slice }}
{{- if $includePublications }}
  {{- $publications = index hugo.Data.publications site.Language.Lang | default (slice) }}
{{- end }}
{{- $discoveries := slice }}
{{- if $includeDiscoveries }}
  {{- $discoveries = index hugo.Data.discoveries site.Language.Lang | default (slice) }}
{{- end }}
{{- $displayTitle := .Title }}
{{- if eq site.Language.Lang "zh" }}
  {{- if eq $displayTitle "Categories" }}
    {{- $displayTitle = "分类" }}
  {{- else if eq $displayTitle "Tags" }}
    {{- $displayTitle = "标签" }}
  {{- end }}
{{- end }}
{{- printf "<?xml version=\"1.0\" encoding=\"utf-8\" standalone=\"yes\"?>" | safeHTML }}
<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/">
  <channel>
    <title>{{ if .IsHome }}{{ site.Title }}{{ else }}{{ $displayTitle }}{{ if eq site.Language.Lang "zh" }} | {{ else }} on {{ end }}{{ site.Title }}{{ end }}</title>
    <link>{{ .Permalink }}</link>
    <description>{{ if eq site.Language.Lang "zh" }}{{ if .IsHome }}最新内容，来自{{ site.Title }}{{ else }}{{ $displayTitle }}的最新内容，来自{{ site.Title }}{{ end }}{{ else }}Recent content {{ if not .IsHome }}in {{ .Title }} {{ end }}on {{ site.Title }}{{ end }}</description>
    {{- with site.Params.images }}
    <image>
      <title>{{ site.Title }}</title>
      <url>{{ index . 0 | absURL }}</url>
      <link>{{ index . 0 | absURL }}</link>
    </image>
    {{- end }}
    <generator>Hugo</generator>
    <language>{{ site.Language.Locale }}</language>{{ with $authorEmail }}
    <managingEditor>{{.}}{{ with $authorName }} ({{ . }}){{ end }}</managingEditor>{{ end }}{{ with $authorEmail }}
    <webMaster>{{ . }}{{ with $authorName }} ({{ . }}){{ end }}</webMaster>{{ end }}{{ with site.Copyright }}
    <copyright>{{ . | markdownify | plainify | strings.TrimPrefix "© " }}</copyright>{{ end }}{{ if and (not .Date.IsZero) (gt (len $pages) 0) }}
    <lastBuildDate>{{ (index $pages.ByLastmod.Reverse 0).Lastmod.Format "Mon, 02 Jan 2006 15:04:05 -0700" | safeHTML }}</lastBuildDate>{{ end }}
    {{- with .OutputFormats.Get "RSS" }}
    {{ printf "<atom:link href=%q rel=\"self\" type=%q />" .Permalink .MediaType | safeHTML }}
    {{- end }}
    {{- range $pages }}
    {{- if and (ne .Layout `search`) (ne .Layout `archives`) }}
    <item>
      <title>{{ .Title }}</title>
      <link>{{ .Permalink }}</link>
      <pubDate>{{ .PublishDate.Format "Mon, 02 Jan 2006 15:04:05 -0700" | safeHTML }}</pubDate>
      {{- with $authorEmail }}<author>{{ . }}{{ with $authorName }} ({{ . }}){{ end }}</author>{{ end }}
      <guid>{{ .Permalink }}</guid>
      <description>{{ with .Description | html }}{{ . }}{{ else }}{{ .Summary | html }}{{ end -}}</description>
      {{- if and site.Params.ShowFullTextinRSS .Content }}
      <content:encoded>{{ (printf "<![CDATA[%s]]>" .Content) | safeHTML }}</content:encoded>
      {{- end }}
    </item>
    {{- end }}
    {{- end }}
    {{- range $publications }}
    {{- $primaryURL := "" }}
    {{- with index .links 0 }}
      {{- $primaryURL = .url }}
    {{- end }}
    {{- if $primaryURL }}
    <item>
      <title>{{ .title }}</title>
      <link>{{ $primaryURL | absLangURL }}</link>
      {{- with .first_public_date }}
      <pubDate>{{ (time.AsTime .).Format "Mon, 02 Jan 2006 15:04:05 -0700" | safeHTML }}</pubDate>
      {{- end }}
      <guid>{{ $primaryURL | absLangURL }}</guid>
      <category>{{ cond (eq site.Language.Lang "zh") "论文" "Publications" }}</category>
      <description>{{ with .summary | html }}{{ . }}{{ end -}}</description>
    </item>
    {{- end }}
    {{- end }}
    {{- range $discoveries }}
    {{- $primaryURL := "" }}
    {{- with index .links 0 }}
      {{- $primaryURL = .url }}
    {{- end }}
    {{- if $primaryURL }}
    <item>
      <title>{{ .title }}</title>
      <link>{{ $primaryURL | absLangURL }}</link>
      <guid>{{ $primaryURL | absLangURL }}</guid>
      <category>{{ cond (eq site.Language.Lang "zh") "发现" "Discoveries" }}</category>
      <description>{{ with .summary_public | html }}{{ . }}{{ end -}}</description>
    </item>
    {{- end }}
    {{- end }}
  </channel>
</rss>
